# Dependencies for remote SSH submission
# Install with: pip install -r requirements_remote.txt

# Option 1: Use pexpect (recommended, better compatibility)
pexpect>=4.8.0

# Option 2: Use paramiko (alternative, more Pythonic)
# paramiko>=2.12.0

# Note: You only need to install ONE of the above:
# - pexpect: Better for interactive passphrase input
# - paramiko: Better for programmatic SSH operations
